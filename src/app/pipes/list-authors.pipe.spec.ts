import { ListAuthorsPipe } from './list-authors.pipe';

describe('ListAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
